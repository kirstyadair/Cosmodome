using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

// Sorry I know this doesn't need its own class, just didn't want to mess with anything in their own scripts
public class ExitButtonScript : MonoBehaviour
{
    [SerializeField] Image exitButtonImage;

    // Update is called once per frame
    void Update()
    {
        
    }
}
